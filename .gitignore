# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
.eggs/
*.egg-info/
*.egg
build/
dist/

# Test artifacts
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover
.hypothesis/
.tox/

# Editor
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Secrets (BELT AND BRACES — never commit credentials)
*credentials*
*.env
.env
secrets.yaml
*.pem
*.key

# Local archives (these belong in <ha_config>/dreame_a2_mower/, not the repo)
sessions/
lidar/
mqtt_archive/

# Auto-generated, data-dependent: tools/inventory_audit.py regenerates this
# from local probe/cloud dumps, so its content differs per machine (and is
# empty in CI where no dumps exist). Generate locally; don't track.
docs/research/inventory/generated/coverage-report.md

# NFS leftover files (created when a process holds a deleted file)
.nfs*
